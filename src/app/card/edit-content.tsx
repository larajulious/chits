import { EmptyState, Screen } from '@/components/ui/primitives';
export default function EditContentWebScreen() { return <Screen><EmptyState title="Editing lives in the app" description="Open Chits on a device to edit card content." /></Screen>; }

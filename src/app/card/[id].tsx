import { EmptyState, Screen } from '@/components/ui/primitives';
export default function CardWebScreen() { return <Screen><EmptyState title="Cards live in the app" description="Open Chits on a device to view locally stored card details." /></Screen>; }

import { EmptyState, Screen } from '@/components/ui/primitives';

export default function BoardWebScreen() {
  return <Screen><EmptyState title="Boards live in the app" description="Open Chits on a device to view locally stored cards." /></Screen>;
}
